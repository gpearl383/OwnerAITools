# OwnerAI Tools — Landing Page

Single-file landing page for owneraitools.com. All HTML/CSS/JS lives in `index.html` — no build step, no dependencies.

Before publishing, swap the placeholders flagged at the top of `index.html`: demo phone number, booking link, and contact email.
